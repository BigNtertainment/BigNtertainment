type Props = {
	elementsAmount: number;
	page: number;
	setPage: (page: number) => void;
};

const Paginator = ({ elementsAmount, page, setPage }: Props) => {
	return <div>Paginator</div>;
};

export default Paginator;

import { cn } from "@/lib/utils/tailwind";
import React from "react";

const PostMap = ({ className }: { className?: string }) => {
	return <div className={cn("", className)}>PostMap</div>;
};

export default PostMap;
